name: DCO report

on:
  workflow_run:
    workflows: ["DCO"]
    types:
      - completed

permissions:
  pull-requests: write

jobs:
  comment:
    name: Organization
    uses: bgilbert/openslide-python/.github/workflows/dco-report.yml@main
